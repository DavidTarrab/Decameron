# Firebase Rules

### Firestore

To test the Firestore rules, run this command: 

	firebase emulators:exec --only firestore "npm test"


To upload the Firestore rules, run this command:

	firebase deploy --only firestore:rules