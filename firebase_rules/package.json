{
  "name": "firebase-rules",
  "version": "1.0.0",
  "scripts": {
    "test-firestore": "mocha --parallel --slow 200 test/firestore.js",
    "test": "mocha --parallel --slow 200 test/*.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^1.1.6",
    "firebase-admin": "^9.4.2",
    "firebase-tools": "^9.0.1",
    "mocha": "^8.0.1"
  }
}
